const SCRIPTS_PR_URL = "http://www.google.com";

const MICROSERVICES = [
];

const SCRIPTS = [
    {
        db: "database-name"
        , prs: [
        ]
    }
];

module.exports = {
    MICROSERVICES,
    SCRIPTS,
    SCRIPTS_PR_URL
};
